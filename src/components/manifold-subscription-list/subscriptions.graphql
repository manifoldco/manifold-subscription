query Subscriptions($owner: ProfileIdentity!) {
  subscriptions(first: 10, owner: $owner) {
    edges {
      node {
        plan {
          displayName
          cost
          configurableFeatures(first: 1) {
            edges {
              node {
                displayName
              }
            }
          }
        }
      }
    }
  }
}
